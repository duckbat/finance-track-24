type Tag {
    tag_id: ID!
    name: String!
}

type Message {
    message: String!
}

type Query {
    tags : [Tag]
}

input TagInput {
    tag_name: String!
}

type Mutation {
    createTag(tag: TagInput!): Tag!
    deleteTag(tag_id: ID!): Message!
}
